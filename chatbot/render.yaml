services:
  - type: web
    name: YourServiceName
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python master.py
    runtime: python
    pythonVersion: 3.10 # Replace with a version compatible with your requirements
